# Examples

The alloconda repo includes example extension modules:

- `python/zigadd`: a minimal extension with tests and type stubs.
- `python/zigzon`: a small ZON codec example with tests and type stubs.

These are good reference points for real-world layouts and packaging.
