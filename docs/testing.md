# Testing & lint

Use the repo recipes to run tests for the example modules:

```bash
just zigadd
just zigzon
```

Linting:

```bash
just lint
```

CLI typing checks:

```bash
cd python/alloconda && ty check
```
